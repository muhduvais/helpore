{
    "build": {
      "command": "npm run build",
      "env": {
        "NODE_ENV": "production"
      }
    },
    "routes": [
      { "handle": "filesystem" },
      { "src": "/.*", "dest": "/" }
    ]
  }